<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFF"
    android:id="@+id/content_resources"
    tools:context="edu.mit.mitmobile2.resources.ResourcesActivityNoModule" >


    <RelativeLayout
        android:id="@+id/resourceInfo"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/resourceInfoText"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:text="@string/resource_info_text" />

    </RelativeLayout>

        <TextView
            android:id="@+id/resourceListResults"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="10dip"
            android:paddingBottom="10dip"
            android:paddingLeft="20dip"
            android:paddingRight="20dip"
            android:text="LIST RESULTS" />

        <ListView
            android:id="@+id/resourceListView"
            android:padding="0dip"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:layout_gravity="start"
            android:choiceMode="singleChoice"
            android:dividerHeight="0dp"
            android:scrollbars="none"
            android:visibility="gone"/>

</LinearLayout>
